[project]
name = "treemovbackend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "shared",
]

[tool.uv.workspace]
members = [
  "services/*",
  "shared"
]

[tool.uv.sources]
shared = { workspace = true }

